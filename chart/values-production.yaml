replicaCount: 3
image:
  tag: "1.0.0"
env:
  - name: APP_ENV
    value: production
hpa:
  minReplicas: 3
  maxReplicas: 10
pdb:
  minAvailable: 2
ingress:
  enabled: true
  host: app.example.com
  tls:
    enabled: true
    secretName: production-kubernetes-app-tls
